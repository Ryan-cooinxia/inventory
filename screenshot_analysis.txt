=== 图1 ===
根据您提供的截图（URL 为 `127.0.0.1:5000/ozon/adaptation/49`），这是一个本地部署的 **Ozon 商品适配工作台** 页面，用于将商品信息（如 SKU、图片、属性）适配至 Ozon 平台。以下是对**中间栏**与**左侧栏**内容的详细结构化描述，严格依据截图可见信息整理：

---

### 🔹 中间栏（主体操作区）

该区域为 SKU 信息填写与审核核心区域，共列出 **6 个 SKU**（SKU 1–6），每个 SKU 均为“XX XPo发射器”类商品（推测为“XPro”或“XPo”型号的发射器设备）。各 SKU 区块结构一致，包含以下字段与控件：

#### ✅ 输入框（可编辑字段）：
1. **颜色**（文本输入框）  
   - 示例值：  
     - SKU 1：`索尼XPo发射器`  
     - SKU 2：`佳能XPo发射器`  
     - SKU 3：`尼康XPo发射器`  
     - SKU 4：`富士XPo发射器`  
     - SKU 5：`奥林巴斯XPo发射器`  
     - SKU 6：`宾得XPo发射器`  
   > 注：此处“XPo”疑似为“XPro”笔误或特定命名；实际应为各品牌相机兼容的无线触发器。

2. **尺寸**（文本输入框）—— *全部为空*

3. **款式**（文本输入框）—— *全部为空*

4. **套装**（数字输入框，默认值 `1`）—— 所有 SKU 均填 `1`

5. **采购价**（货币输入框，带“¥”符号）—— *全部为空*（仅显示占位符“¥”）

6. **SKU 审实确认**（复选框）—— 每个 SKU 下方均有勾选状态：  
   - SKU 1–6 均已勾选 ✅（显示“✓ 已确认”）

7. **分组中… / 会开卖试**（底部按钮）  
   - 蓝色按钮：“分组中…”（灰色禁用态）  
   - 色按钮：“会开卖试”（未激活）

8. **材质 / 重量 / 电池容量 / 包装内含明细等**（多行文本区域）  
   - 底部有 `[“材质”, “重量”, “电池容量”, “包装内含明细”]` 提示，但当前无内容输入。

#### ❗ 证据列表 / 识别结果？
- **无独立“证据列表”面板**。
- 但存在一个**弹窗提示**（覆盖在中间栏上方）：
  > `127.0.0.1:5000 显示`  
  > `分析完成: 文本13条, 图片0张, 冲突0个`  
  > （蓝色“确定”按钮）
  - 此为**OCR/文本识别任务完成反馈**，说明系统已自动解析了原始数据（13条文本信息），未识别到图片，无冲突项。
  - **这是唯一的“识别结果”呈现**，非表格或列表形式，仅为摘要提示。

#### 📋 可见字段汇总（每 SKU 公共字段）：
| 字段名 | 是否可见 | 示例值/状态 |
|--------|----------|-------------|
| SKU 编号 | 是 | SKU 1 ~ SKU 6 |
| 颜色 | 是 | 各品牌+“XPo发射器” |
| 尺寸 | 是（空） | — |
| 款式 | 是（空） | — |
| 套装数量 | 是 | 全为 `1` |
| 采购价 | 是（空） | ¥ |
| 已确认 | 是 | 全部勾选 |
| 分组/上架状态 | 是 | “分组中…”（禁用） |

> ⚠️ 注意：右侧栏有“保存属性”按钮、错误提示（红色 × 字段）、Ozon 视频链接等，但按题意不在此列。

---

### 🔹 左侧栏（源素材与产品概览区）

#### 1. **源图片区域**
- 标题：`源 SKU (6)` → 表明共 6 个原始 SKU 来源。
- 下方列出 6 行商品条目，每行为：
  - **主图缩略图**（小图，清晰可见设备外观）
  - **商品名称**（加粗黑体）  
    例：`索尼XPo发射器`、`佳能XPo发射器`……（同中间栏）
  - **规格描述**（灰色小字）  
    例：`颜色:索尼xpro发射器 | 款式:-`  
    > 注：此处“xpro”小写，与中间栏“XPo”大小写不一致，可能为原始数据差异。
  - 所有 6 条均完整展示，无缺失。

#### 2. **源图片下方是否有产品详情区域？**
✅ **有**，紧接在“源 SKU”下方，标题为：  
> **源图片 (24)**

- 显示 **24 张缩略图**（当前可见 6 张 + “+16” 按钮），布局为 2×3 网格（前 6 张可见）：
  - 图片内容：均为不同角度/组合的发射器实物图（含手柄、接收器、包装盒等）
  - 每张图下方无文字标签（纯图像预览）
- 下方有一个蓝色按钮：  
  > `+ 手动补充图片`  
  （带下载图标，用于上传额外图片）

#### 3. **哪些信息已显示？**
| 项目 | 是否显示 | 说明 |
|------|----------|------|
| 源 SKU 列表（6条） |  | 含名称、颜色、款式字段（部分为“-”） |
| 每 SKU 对应主图缩略图 | ✅ | 6 张清晰设备图 |
| 源图片库（24张） | ✅ | 当前展示前6张 + “+16”提示 |
| 手动补图入口 | ✅ | “+ 手动补充图片”按钮 |
| SKU 编号标识 | ✅ | “源 SKU (6)”明确数量 |

#### 4. **哪些信息缺失？**
| 项目 | 是否缺失 | 原因/表现 |
|------|----------|-----------|
| 产品详细参数（

=== 图2 ===
根据您提供的截图（适配工作台页面），以下是对**中间栏**和**左侧栏**内容的详细描述，严格依据图中可见信息整理：

---

### ✅ **中间栏内容详述**

中间栏是核心操作与结果展示区，自上而下结构清晰：

#### 1. **SKU基础信息输入区**
- 标题：`SKU 6: 宾得XPro发射器`
- 输入框（共5个）：
  - `颜色`：文本输入框，已填入“宾得XPro发射器”（注：此处应为颜色值，但实际填入了产品名，属异常或占位）
  - `尺寸`：空文本输入框
  - `款式`：空文本输入框
  - `套装`：数值输入框，当前值为 `1`
  - `采购价`：带货币符号“¥”的数值输入框，为空

- 右上角有开关控件：
  - `已确认`：灰色关闭状态（未勾选）

#### 2. **操作按钮区**
- 蓝色按钮：`🔍 批量分析`
- 灰色按钮：`合并售卖`

#### 3. **证据列表（13 条）**
- 标题：`证据列表 (13 条)`，带折叠/展开图标（当前展开）
- 每条记录含字段（从左到右）：
  - 字段路径（如 `product_identity.name`, `skus[1].bundle_quantity` 等）
  - 提取状态标签：黄色背景 `extracted` + 置信度（如 `90% [text]`）
  - 值内容（示例）：
    - `product_identity.name` → “神牛xpro-c发射器无线引闪器相机闪光灯内置2.4GX系统闪光灯配件”
    - `skus[1].bundle_quantity` → `1`
    - `skus[1].color` → “黑尼xpro发射器”（疑似OCR错误，“黑尼”应为“宾得”？）
    - `skus[2].bundle_quantity` → `1`
  - 操作图标（每行右侧）：
    - ✔️ 绿色对勾（确认）
    - ❌ 红色叉号（驳回）
    - ▲▼ 折叠箭头（部分行可展开更多字段）

> 注：共列出13条证据，图中仅显示前5–6条，其余被折叠或滚动隐藏。

#### 4. **视觉识别结果（10 条）**
- 标题：`🔍 视觉识别结果 (10 条)`，带折叠图标（当前展开）
- 每条记录含：
  - 图标（类似功能模块图标）
  - `functions` 标签：绿色背景 `100%`（表示置信度100%）
  - 主识别项（如）：
    - `wireless trigger 2.4G`
    - `无线引闪 2.4G`（重复出现3次，可能为不同角度/区域识别）
  - 同样配有 ✔️ / ❌ 操作按钮

#### 5. **未知字段 / 待确认区**
- 标题：`⚠️ 未知字段 / 确认`
- 内容：`[“材质”, “重量”, “电池容量”, “包装内容明细”]`
  - 表明这些字段尚未被自动提取或需人工补充

---

### ✅ **左侧栏内容详述**

左侧栏为图像与产品信息展示区（图中仅显示顶部部分，下方被截断）：

#### 1. **源图片区域**
- 显示一张商品主图（推测为“宾得XPro发射器”实物图），但因截图裁剪，**图片本身未完整显示**，仅见顶部边缘。
- 图片下方**无独立“产品详情区域”标题栏**，但存在信息块：

#### 2. **已显示的信息项（可见部分）**
- 无明确“产品详情”区块标题，但紧接图片下方有：
  - SKU编号：`SKU 6`
  - 产品名称（隐含）：通过中间栏反推为“宾得XPro发射器”，左侧未直接显示该名称文本。
  - **实际左侧栏在截图中几乎空白**——除顶部图片外，下方大面积留白，**未显示任何结构化产品详情字段**（如品牌、型号、规格参数等）。

#### 3. **缺失的信息项（对比中间栏与常规需求）**
左侧栏**完全缺失以下常见产品详情信息**：
- 品牌（应为“宾得”）
- 型号（如 XPro-T / XPro-R 等具体子型号）
- 类别（如“无线引闪器”、“触发器”）
- 技术参数（2.4G频段、兼容机型、续航、尺寸重量等）
- 包装清单（是否含电池、线缆、收纳袋等）
- 供应商/产地信息
- 商品主图以外的多角度图/细节图（当前仅1张图且不完整）

> 🔍 综合判断：左侧栏当前仅承载**源图片占位**，**未实现产品详情结构化展示**；所有关键属性均依赖中间栏的“证据列表”与“视觉识别结果”进行人工核验与补全。

---

### 📌 总结清单（按要求逐项列出）

#### 中间栏：
1. 输入框共5个：颜色、尺寸、款式、套装、采购价  
2. 有证据列表（13条），含字段：`product_identity.name`, `skus[i].bundle_quantity`, `skus[i].color` 等  
3. 有视觉识别结果（10条），含字段：`wireless trigger 2.4G`, `无线引闪 2.4G` 等  
4. 可见字段包括：提取值、置信度（%）、字段路径、操作按钮（✔️/❌）  
5. 存在“未知字段”待确认区，列出4项缺失字段  

#### 左侧栏：
1. 源图片下方**无独立产品详情区域**（无标题、无表格/卡片布局）  
2. 已显示信息：仅源图片（不完整）、SKU编号（隐含）  
3. 缺失信息（明确未显示）：
   - 品牌名称  
   - 具体型号  
   - 分类/用途  
   - 技术参数（频率、兼容性、电池等）  
   - 包装内容  
   - 多图展示  
   - 供应商/认证信息  

--- 

如需进一步分析字段映

